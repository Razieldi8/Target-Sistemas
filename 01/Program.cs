int INDICE = 13, SOMA = 0, K = 0;

// Enquanto K < INDICE
Enquanto K < INDICE faça {
    K = K + 1;       // Incrementa o valor de K
    SOMA = SOMA + K; // Soma o valor atual de K a SOMA
}

// Imprime o valor de SOMA
Imprimir(SOMA);

SOMA = 91;